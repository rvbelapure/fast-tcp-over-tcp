#include <stdlib.h>
#include "sdis_queue.h"

socket_list * create_socket_list(){
	socket_list *list = malloc(sizeof(socket_list));
	list->head = NULL;
	list->list_size = 0;
}

void add_socket(socket_list *){
	socket_list_node 
}
